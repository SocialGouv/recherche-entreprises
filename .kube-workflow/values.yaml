app-front:
  enabled: true
  imagePackage: front
  containerPort: 8080

app-api:
  enabled: true
  imagePackage: api
  host: "api-{{ .Values.global.host }}"
  envFrom:
    - secretRef:
        name: elastic-recherche-entreprises-read
